Regenerate the usage tracking dashboard and open it in the browser.

Steps:
1. Run the dashboard generator: `bash ~/.claude/rate-limit-status/generate-dashboard.sh`
2. Open the dashboard: `open ~/.claude/usage-dashboard.html`
3. Tell me the dashboard has been opened and summarize the current usage stats from the JSONL data.
